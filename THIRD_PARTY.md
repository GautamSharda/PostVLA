# Third-Party Software

This repository pins but does not relicense:

- OpenPI: `third_party/openpi`
- RLinf: `third_party/RLinf`
- FlashRT: `third_party/FlashRT`
- The SO100 MuJoCo model fork: `third_party/mujoco_menagerie`

Each submodule retains its upstream license. `patches/` and `overlays/` describe the local
integration changes without vendoring complete third-party repositories.
